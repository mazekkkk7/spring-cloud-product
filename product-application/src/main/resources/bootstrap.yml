spring:
  application:
    name: spring-cloud-product
  cloud:
    config:
      uri: http://localhost:9921
      profile: dev
      label: master
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
debug: true
